<?xml version="1.0" encoding="utf-8"?>

<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
             android:layout_width="fill_parent"
             android:layout_height="wrap_content"
             android:padding="@dimen/widget_margin">

    <LinearLayout
        android:id="@+id/content_container"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="center_horizontal"
        android:background="@color/notesBackgroundColor"
        android:clickable="false"
        android:orientation="horizontal"
        android:gravity="center_horizontal">

        <ImageView
            android:id="@+id/action_note_widget"
            android:layout_width="68dp"
            android:layout_height="48dp"
            android:padding="2dp"
            android:src="@android:drawable/ic_menu_add"
            android:layout_gravity="center"
            android:contentDescription="@string/add_a_note"/>

        <ImageView
            android:id="@+id/action_list_note_widget"
            android:layout_width="68dp"
            android:layout_height="48dp"
            android:src="@android:drawable/ic_menu_sort_by_size"
            android:layout_gravity="center"
            android:contentDescription="@string/add_a_list_note"/>
    </LinearLayout>

</RelativeLayout>